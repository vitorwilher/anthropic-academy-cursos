# Artefatos de build do Quarto
/.quarto/
*_files/
_site/

# macOS
.DS_Store

# Os slides .html são GERADOS pelo Quarto no GitHub Action (publish.yml) e publicados
# no Pages — não precisam ser versionados. O index.html (escrito à mão) é versionado.
slides/*/slides.html
